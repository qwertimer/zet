# Using ansible-vault encrypt is the simplest way to configure
encryption

When working with ansible individual files can be enrypted using
`ansible-vault encrypt foo.yml bar.yml baz.yml`

This is a simple way to configure encrypted files to work with an
ansible setup

